# configuração da aplicação
monitor:
  connectionType: WebSocket ## WebSocket ou Http
  requestIntervalSeconds: 5 ## only in HttpMode

# TODO: essa parte provavelmente n vai ser usanda colocanqui aqui pro simplicidade,
# as URLs ficaram internamente no codigo, pois, tem que buildas as stringp, de request e sera suportada um tipo de API somente.
# api:
#   httpUrl: "https://api.example.com/quote"
#   webSocketUrl: "wss://api.example.com/stream"

smtp:
  host: "smtp-relay.brevo.com"
  port: 587
  useSsl: true
  from: "vitorr2009@gmail.com"
  to: "vitorr2009@gmail.com"

alerts:
  sendIntervalSeconds: 5
  maxQueuedEvents: 10
